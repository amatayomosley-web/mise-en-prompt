{
  "$schema-comment": "Project-local Claude Code settings for mise-en-prompt. SessionStart installs the package so the enforcement hooks can import it. PreToolUse hooks enforce the chef agent's state contract: state.yaml writes must validate against the schema and respect research_log ordering; recipe.md writes are blocked until state.phase == GUIDE.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -m mise_en_prompt.hooks.state_validate",
            "timeout": 5000
          }
        ]
      },
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -m mise_en_prompt.hooks.phase_check",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
